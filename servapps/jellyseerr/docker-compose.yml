cosmos-installer: null
services:
  app:
    image: fallenbagel/jellyseerr:2.7.3@sha256:4538137bc5af902dece165f2bf73776d9cf4eafb6dd714670724af8f3eb77764 # Image to use for the jellyseerr service
    ports:
      - "5055:5055" # Maps port 5055 on the host to port 5055 on the container
    volumes:
      - big-bear-jellyseerr_config:/app/config # Binds a volume from host to container for configuration
    environment:
      - LOG_LEVEL=debug # Sets the log level to debug
      - TZ=$TZ # Sets the timezone, using host's timezone variable
    restart: unless-stopped # Service will restart unless it is explicitly stopped
    network_mode: bridge # Uses bridge network mode
volumes:
  big-bear-jellyseerr_config:
    name: big-bear-jellyseerr_config
    driver: local
minVersion: 0.14.0
